#include <bits/stdc++.h> 
using namespace std;  
int isPrime(int N){  
    int i = 2;  
    int k = ceil(sqrt(N)); 
    while(i<= k){ 
        if(N % i == 0) 
            return 0; 
        i += 1; 
    } 
    return 1; 
} 